DELIMITER //

CREATE TRIGGER trigger_set_sgi_number
BEFORE INSERT ON plan_sgi
FOR EACH ROW
BEGIN
    IF NEW.number IS NULL THEN
        SET NEW.number = (SELECT COALESCE(MAX(number), 0) + 1 FROM plan_sgi);
    END IF;
END; //

DELIMITER ;