package com.example.rces.models.enums;

public enum Role {
    USER, ADMIN, TECHNOLOGIST, OTK, CONSTRUCTOR, MASTER, CONTROL, EVENT, SYSTEM;

    public String getName() {
        return this.name();
    }
}
